<div class="burger">
	<span class="burger-name">{{burger_name}} </span>
	<button class="btn btn-primary btn-xs devour" type="submit" data-id="{{id}}" data-devour="{{devoured}}">Devour It!</button>
</div>